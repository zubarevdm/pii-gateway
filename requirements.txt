# --- API / proxy ---
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- RU NER ---
# Natasha = razdel + navec + slovnet + yargy. Всё работает локально, без сети.
natasha>=1.6
